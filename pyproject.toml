[project]
name = "reyserver"
version = "1.1.35"
description = "API method set."
readme = "README.md"
requires-python = ">=3.12"
license = {file = "LICENSE"}
keywords = ["rey", "reyxbo", "api", "backend", "async", "asynchronous"]
authors = [
    {name = "Rey", email = "reyxbo@163.com"}
]
dependencies = [
    "reykit"
]

[project.urls]
homepage = "https://github.com/reyxbo/reyserver/"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.workspace]
members = [
    "python-multipart",
    "reyserver",
    "sqlalchemy",
    "sqlmodel",
    "uvicorn[standard]"
]
